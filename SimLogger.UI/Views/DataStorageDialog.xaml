<Window x:Class="SimLogger.UI.Views.DataStorageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Icon="pack://application:,,,/golfball.ico"
        Title="Data Storage Location"
        Height="350" Width="550"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Style="{StaticResource MaterialDesignWindow}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="Data Storage Location"
                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                   Margin="0,0,0,8"/>

        <!-- Description -->
        <TextBlock Grid.Row="1"
                   Text="Select the folder where Sim Logger will store the database and synced videos. Changing this location will not migrate existing data."
                   TextWrapping="Wrap"
                   Opacity="0.7"
                   Margin="0,0,0,16"/>

        <!-- Path Selection -->
        <Grid Grid.Row="2" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="PathTextBox"
                     Grid.Column="0"
                     IsReadOnly="True"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     materialDesign:HintAssist.Hint="Storage Path"
                     VerticalContentAlignment="Center"/>

            <Button Grid.Column="1"
                    Content="Browse..."
                    Click="BrowseButton_Click"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Margin="8,0,0,0"/>
        </Grid>

        <!-- Current location info -->
        <StackPanel Grid.Row="3" Orientation="Vertical" Margin="0,0,0,8">
            <TextBlock x:Name="CurrentPathText"
                       Style="{StaticResource MaterialDesignCaptionTextBlock}"
                       Opacity="0.5"
                       TextWrapping="Wrap"
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock x:Name="DefaultPathText"
                       Style="{StaticResource MaterialDesignCaptionTextBlock}"
                       Opacity="0.5"
                       Margin="0,4,0,0"
                       TextWrapping="Wrap"
                       TextTrimming="CharacterEllipsis"/>
        </StackPanel>

        <!-- Spacer -->
        <Grid Grid.Row="4"/>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Reset to Default"
                    Click="ResetButton_Click"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Margin="0,0,8,0"/>
            <Button Content="Cancel"
                    Click="CancelButton_Click"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Margin="0,0,8,0"/>
            <Button x:Name="OkButton"
                    Content="OK"
                    Click="OkButton_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</Window>
