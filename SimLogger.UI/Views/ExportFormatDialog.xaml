<Window x:Class="SimLogger.UI.Views.ExportFormatDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Icon="pack://application:,,,/golfball.ico"
        Title="Export Format"
        Height="280" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Style="{StaticResource MaterialDesignWindow}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="Select Export Format"
                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                   Margin="0,0,0,16"/>

        <!-- Format Options -->
        <StackPanel Grid.Row="1" Margin="0,0,0,16">
            <RadioButton x:Name="GSProFormatRadio"
                         GroupName="ExportFormat"
                         IsChecked="True"
                         Margin="0,0,0,8"
                         Style="{StaticResource MaterialDesignRadioButton}">
                <StackPanel>
                    <TextBlock Text="GSPro Format"
                               FontWeight="Medium"/>
                    <TextBlock Text="Compatible with tools that import GSPro CSV files"
                               FontSize="12"
                               Foreground="{DynamicResource MaterialDesignBodyLight}"
                               TextWrapping="Wrap"/>
                </StackPanel>
            </RadioButton>

            <RadioButton x:Name="ShotPatternFormatRadio"
                         GroupName="ExportFormat"
                         Margin="0,8,0,0"
                         Style="{StaticResource MaterialDesignRadioButton}">
                <StackPanel>
                    <TextBlock Text="Shot Pattern Format"
                               FontWeight="Medium"/>
                    <TextBlock Text="Compatible with the Shot Pattern app"
                               FontSize="12"
                               Foreground="{DynamicResource MaterialDesignBodyLight}"
                               TextWrapping="Wrap"/>
                </StackPanel>
            </RadioButton>
        </StackPanel>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Cancel"
                    Click="CancelButton_Click"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Margin="0,0,8,0"/>
            <Button Content="Export"
                    Click="ExportButton_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</Window>
