<Window x:Class="SimLogger.UI.Views.RealtimePortDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Icon="pack://application:,,,/golfball.ico"
        Title="Configure Real-time Detection"
        Height="320" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Style="{StaticResource MaterialDesignWindow}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="Configure GSPro API Port"
                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                   Margin="0,0,0,16"/>

        <!-- Port Input -->
        <TextBox x:Name="PortTextBox"
                 Grid.Row="1"
                 materialDesign:HintAssist.Hint="GSPro API Port (e.g., 12321)"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 PreviewTextInput="PortTextBox_PreviewTextInput"
                 Margin="0,0,0,16"/>

        <!-- Description -->
        <TextBlock Grid.Row="2"
                   TextWrapping="Wrap"
                   Opacity="0.7"
                   VerticalAlignment="Top">
            <Run Text="This is the port GSPro uses for launch monitor communication."/>
            <LineBreak/>
            <LineBreak/>
            <Run Text="Common ports: 12321 (ProTee), 921 (OpenConnect API)"/>
            <LineBreak/>
            <LineBreak/>
            <Run Text="Requires Npcap and admin privileges." FontStyle="Italic"/>
        </TextBlock>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Cancel"
                    Click="CancelButton_Click"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Margin="0,0,8,0"/>
            <Button x:Name="OkButton"
                    Content="OK"
                    Click="OkButton_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</Window>
