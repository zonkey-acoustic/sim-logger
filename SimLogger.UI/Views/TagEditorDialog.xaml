<Window x:Class="SimLogger.UI.Views.TagEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Icon="pack://application:,,,/golfball.ico"
        Title="Edit Tags"
        Height="500" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Style="{StaticResource MaterialDesignWindow}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="Manage tags for this shot"
                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                   Margin="0,0,0,16"/>

        <!-- Add Tag Input -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="NewTagTextBox"
                     Grid.Column="0"
                     materialDesign:HintAssist.Hint="New tag name"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     KeyDown="NewTagTextBox_KeyDown"
                     Margin="0,0,8,0"/>

            <Button Grid.Column="1"
                    Content="Add"
                    Click="AddTagButton_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    VerticalAlignment="Center"/>
        </Grid>

        <!-- Current Tags Label -->
        <TextBlock Grid.Row="2"
                   Text="Current tags"
                   Style="{StaticResource MaterialDesignBody2TextBlock}"
                   Opacity="0.7"
                   Margin="0,0,0,8"/>

        <!-- Current Tags List -->
        <ScrollViewer Grid.Row="3"
                      VerticalScrollBarVisibility="Auto"
                      Margin="0,0,0,12">
            <ItemsControl x:Name="CurrentTagsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource MaterialDesign.Brush.Card.Background}"
                                CornerRadius="4"
                                Padding="12,6"
                                Margin="0,0,0,4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0"
                                           Text="{Binding}"
                                           VerticalAlignment="Center"
                                           Style="{StaticResource MaterialDesignBody1TextBlock}"/>

                                <Button Grid.Column="1"
                                        Click="RemoveTag_Click"
                                        Tag="{Binding}"
                                        Style="{StaticResource MaterialDesignIconButton}"
                                        Width="28" Height="28"
                                        ToolTip="Remove tag">
                                    <materialDesign:PackIcon Kind="Close" Width="16" Height="16"/>
                                </Button>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Existing Tags Section -->
        <StackPanel Grid.Row="4" Margin="0,0,0,16"
                    x:Name="ExistingTagsPanel">
            <TextBlock Text="Quick add from existing tags"
                       Style="{StaticResource MaterialDesignBody2TextBlock}"
                       Opacity="0.7"
                       Margin="0,0,0,8"/>

            <ItemsControl x:Name="ExistingTagsList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding}"
                                Click="ExistingTag_Click"
                                Tag="{Binding}"
                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                Padding="8,2"
                                Margin="0,0,6,6"
                                Height="28"
                                FontSize="12"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Cancel"
                    Click="CancelButton_Click"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Margin="0,0,8,0"/>
            <Button Content="Apply"
                    Click="ApplyButton_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</Window>
